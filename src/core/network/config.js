// const environment = (process.env.NODE_ENV) ? process.env.NODE_ENV : 'DEV';

const configuration = {
  baseURL: "",
  getURL: "/get",
  postURL: "/post",
  token: "",
  environment: "DEV"
};

export default configuration;
