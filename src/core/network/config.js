// const environment = (process.env.NODE_ENV) ? process.env.NODE_ENV : 'DEV';

const configuration = {
	baseURL: 'https://qts4luu328.execute-api.us-east-1.amazonaws.com/Dev/api',
	getURL: '/get',
	postURL: '/post',
	token : '',
	environment: 'DEV'
};

export default configuration;

